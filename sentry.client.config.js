// Intentionally a minimal no-op to prevent any client-side Sentry initialization
// being triggered from this file. Keep this file only if some build tools
// expect it to exist; otherwise it's safe to remove.
module.exports = {};

